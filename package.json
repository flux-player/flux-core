{
  "name": "flux-core",
  "version": "0.0.1",
  "description": "Core components for the Flux project",
  "main": "dist/index.js",
  "repository": "https://github.com/flux-player/flux-core.git",
  "author": "Melvin Mupondori",
  "license": "MIT",
  "types": "dist/types.d.ts",
  "prepublish": "tsc",
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit --coverage --verbose",
    "lint": "tsc --noEmit && eslint \"**/*.{js,ts}\" --quiet --fix",
    "watch-test": "npm run test -- --watchAll"
  },
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0"
  },
  "dependencies": {
    "@types/node": "^13.9.2",
    "@types/text-encoding": "^0.0.35",
    "date-fns": "^2.11.0",
    "dotenv": "^8.2.0",
    "node-persist": "^3.0.5",
    "searchjs": "^1.0.2",
    "text-encoding": "^0.7.0"
  }
}
